{
  "name": "@examples/express-orpc-shield",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm -C .. run build",
    "dev": "tsx ./src/server.ts",
    "prebuild": "pnpm -C .. run build",
    "build": "tsc",
    "prestart": "pnpm -C .. run build",
    "start": "node ./dist/server.js"
  },
  "dependencies": {
    "@orpc/server": "^1.8.5",
    "@orpc/openapi": "^1.8.5",
    "express": "^4.18.2",
    "orpc-shield": "link:..",
    "typescript": "5.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "tsx": "^4.20.5"
  }
}
